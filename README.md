# Voxel Engine - C++ OpenGL

Легкий воксельный движок в стиле Minecraft, написанный на C++ с использованием OpenGL.  
Без лишних библиотек: цветные блоки, контурная обводка, генерация мира, физика.

---

## 📥 Скачать проект

### Вариант 1: Готовый релиз (Рекомендуется) ⭐

Скачайте готовый ZIP-архив с последней версией проекта из раздела **Releases**:

[![Скачать релиз](https://img.shields.io/badge/📦-Скачать%20ZIP-green?style=for-the-badge&logo=github)](../../releases/latest/download/voxel-engine-release.zip)

**В архиве:**
- Все исходные файлы проекта
- Скрипт автоматической сборки `build.sh`
- Makefile для компиляции

### Вариант 2: Клонирование репозитория

```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd voxel-engine
```

Или скачайте ZIP через кнопку **Code → Download ZIP** на GitHub.

---

## 🚀 Быстрый старт (Автоматическая сборка)

> **Вам НЕ НУЖНО** собирать проект вручную! Мы подготовили скрипт, который всё сделает за вас.

### Для Linux и macOS:

1. Распакуйте скачанный ZIP-архив
2. Откройте терминал в папке проекта
3. Запустите скрипт сборки:

```bash
chmod +x build.sh
./build.sh
```

Скрипт автоматически:
- ✅ Определит вашу операционную систему
- ✅ Установит все необходимые зависимости (SDL2, GLEW, OpenGL)
- ✅ Скомпилирует проект

После успешной сборки запустите игру:

```bash
./bin/voxel-gl
```

Или используйте команду:
```bash
make run
```

---

## 🔧 Ручная сборка (Не рекомендуется)

Если вы хотите собрать проект вручную:

### Установка зависимостей (Linux/Ubuntu/Debian)

```bash
sudo apt update
sudo apt install build-essential libsdl2-dev libglew-dev libgl1-mesa-dev make
```

### Команды сборки

```bash
make        # Скомпилировать проект
make run    # Запустить игру
make clean  # Очистить файлы сборки
```

## Управление

- **W, A, S, D** — Перемещение
- **Пробел** — Прыжок
- **Мышь** — Обзор камеры
- **ESC** — Выход

## Особенности

- Процедурная генерация ландшафта с использованием шума Перлина
- Различные типы блоков: трава, земля, камень, вода, песок, дерево, листва
- Физика игрока: гравитация, коллизии, прыжки
- Двухпроходный рендеринг: сплошные цвета + чёрная обводка
- Система чанков 16x32x16

## Технологии

- **C++11**
- **OpenGL 3.3 Core Profile**
- **SDL2** — Окно и ввод
- **GLEW** — Загрузка функций OpenGL

## Лицензия

MIT